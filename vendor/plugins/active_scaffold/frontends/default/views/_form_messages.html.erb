<%= render :partial => 'messages' %>

<% unless @record.nil? %>
  <% p @record.class.name.underscore %>
  <%= error_messages_for :record, :object_name => t("label.#{@record.class.name.underscore}.update_model") %>
<% end %>