<% if @prev -%>
<h1><%= t("label.diff.change_article_title", :article => "#{@article.category}/#{@article.name}", :from => @prev.version, :to => @article.version)%></h1>
<% else -%>
<h1><%= t("label.diff.new_article_title", :article => "#{@article.category}/#{@article.name}")%></h1>
<% end -%>

<div id="diff">
<%= controller.display_diff(@prev, @article, :render_article)%>
</div>
<%= render :partial => "articles/versions"%>
