<%= flashes_if_any %>

<%= error_messages_for :user %>

<% form_for :user, :url => users_path do |f| -%>
  <%= render :partial =>"form", :locals => {:f => f } %>

  <p><%= submit_tag t('label.user.create') %></p>

<% end -%>
