<h1><%= t('label.wiki_page.listing')%></h1>

<table class="data">
  <tr>
    <th><%= t('label.wiki_page.name')%></th>
    <th><%= t('label.wiki_page.user')%></th>
    <th> </th>
    <th> </th>
  </tr>

<% @wiki_pages.each_with_index do |wiki_page, i| %>
  <tr class="<%= (i % 2 == 0) ? 'even' : 'odd' %>">
    <td><%=h wiki_page.name %></td>
    <td><%=h wiki_page.user ? wiki_page.user.full_name : "Unknown" %></td>
    <td><%= link_to t('label.common.show'), wiki_page_path(:id => wiki_page.name || wiki_page.id, :lang => params[:lang]) %></td>
    <td><%= link_to t('label.common.edit'), edit_wiki_page_path(:id => wiki_page.name || wiki_page.id, :lang => params[:lang]) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t('label.wiki_page.new'), new_wiki_page_path(:lang => params[:lang]) %> |
<%= link_to 'RSS', wiki_rss_path(:lang => params[:lang]) %>
